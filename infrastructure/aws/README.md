# AWS Infrastructure

This directory will hold the repeatable API Gateway, Lambda, Secrets Manager,
CloudWatch, throttling, and deployment configuration for the public TestWired
application.

No stack has been deployed from this new repository yet.
